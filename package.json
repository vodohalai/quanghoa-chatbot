{
  "name": "quanghoa-chatbot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute quanghoa-db --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute quanghoa-db --remote --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250525.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.16.0"
  }
}
